package main

const (
	// Path where builded logger would be located after build
	installPath = "/opt/websocket_logger"

	// Address of websocket endpoing where site will be trying to connect
	websocketAddress = "ws://127.0.0.1"
)
